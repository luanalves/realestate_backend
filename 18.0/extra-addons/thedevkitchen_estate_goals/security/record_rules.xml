<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Rule 1: Company isolation — all internal users can only read goals from own company -->
        <record id="rule_estate_goal_company" model="ir.rule">
            <field name="name">Estate Goal: Company isolation</field>
            <field name="model_id" ref="model_thedevkitchen_estate_goal"/>
            <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <!-- Rule 2: Agent own-only restriction — agents may only read/write their own goals -->
        <record id="rule_estate_goal_agent_own" model="ir.rule">
            <field name="name">Estate Goal: Agent sees own goals only</field>
            <field name="model_id" ref="model_thedevkitchen_estate_goal"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('quicksol_estate.group_real_estate_agent'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Feature 022 / ADR-029: SaaS Admin cross-company override -->
        <record id="rule_estate_goal_admin_all" model="ir.rule">
            <field name="name">System Admin: All Estate Goals (Cross-Company)</field>
            <field name="model_id" ref="model_thedevkitchen_estate_goal"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
        </record>

    </data>
</odoo>
