Invoke the `thedevkitchen-speckit-project-constitution` subagent to create or update the project constitution at `CLAUDE.md`.

Pass any argument provided by the user directly to the agent as the source directory (SOURCE_DIR). If no argument is provided, the agent will ask the user interactively.

$ARGUMENTS
