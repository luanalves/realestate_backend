Invoke the `thedevkitchen-speckit-project-knowledge-base` subagent to build or update the project knowledge base with detailed documentation of modules, integrations, environments, infrastructure, security, performance, APIs, scheduled jobs, queues, and testing strategy.

Pass any focus area or source directory as argument (e.g., `modules`, `integrations`, or `src/`). Leave blank for full analysis.

$ARGUMENTS
